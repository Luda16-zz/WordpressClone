<h1>This is my index for posts</h1>

<% @post.each do |post| %>
	<div class="post_wrapper">
		<h2 class="title">
			<%= link_to post.title, post %>
		</h2>
		<p class="data">
			<%= post.created_at.strftime("%B, %d, %Y") %>
		</p>
	</div>
<% end %>